# Legacy tunnel templates removed from runtime

Token-based Cloudflare Tunnel is the only supported production runtime in TT-Production v14.0.

Former config-mode template artifacts were moved to `tt-core/archive/legacy/tt-tunnel-config-mode/` for historical reference only.
Do not use them for new deployments.

