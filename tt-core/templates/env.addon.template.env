# Add-on: <service>
# Pick a port from the add-ons range (20000-22999 suggested).
TT_<SERVICE>_HOST_PORT=20080

# If tunnel enabled, default is routed unless NO_TUNNEL_<SERVICE>=1
# NO_TUNNEL_<SERVICE>=0
